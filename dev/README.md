# Refab Dev Place

This folder is only for optional Rojo Studio place sync while testing Refab
inside Roblox Studio.

Refab itself is built from `plugin.project.json` and installed as a local Studio
plugin. Do not serve `plugin.project.json`.
